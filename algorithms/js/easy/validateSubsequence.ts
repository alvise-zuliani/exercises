// Given two non-empty arrays of integers, write a function that determines whether the second array is a
// subsequence of the first one. Note that a single number in an array and the array itself are both valid
// subsequences of the array

export function isValidSubsequence(array: number[], sequence: number[]) {
    // Write your code here.
    return 
}
  